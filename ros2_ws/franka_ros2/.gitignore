.env
.idea
.vscode
*.pyc
COLCON_IGNORE
!franka_mobile_sensors/COLCON_IGNORE
AMENT_IGNORE
cmake-build-*/
build/
install/
log/
# Upstream ignores these three because it expects them cloned separately.
# This monorepo vendors them instead, so the workspace is complete on clone.
#libfranka
#franka_description
#olvx_descriptions_module