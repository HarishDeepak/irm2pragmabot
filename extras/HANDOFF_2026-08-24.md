# HANDOFF — session of 2026-08-24

**Read `HANDOFF_SESSION.md` first** for the verified facts (calibration, traps,
protected modules). This file covers only what changed on 2026-08-24 and what to
do next. Report due **2026-09-14**.

---

## 0. State in five lines

- Branch `ros2-port`, **8 commits, NOT pushed**. Working tree clean.
- **5 test scripts, 38 checks, all passing**, all robot-free.
- **A1 (logging) done. B4 (perception wiring) done.**
- Nothing has run on the **lab robot**. No real pick has happened.
- Artifact (concept/teaching page):
  https://claude.ai/code/artifact/fd18fc1f-5f22-4789-9364-3c8470884d0d

```bash
cd D:/irm2pragmabot/pragmabot
for t in scripts/test_*.py; do python $t; done   # expect 5x pass
git push origin ros2-port                        # when ready
```

---

## 1. Commits this session

| Commit | What |
|---|---|
| `a0e3510` | Delete dead `grounded_sam.py` + test — unreachable, ROS 1 catkin paths, SAM 1 while the working path is SAM 2 |
| `d6c03aa` | ZMQ perception server + client + guard tests |
| `1e927a6` | **Per-trial JSONL logging (A1)** |
| `7821571` | Track `extras/` docs in git; correct the never-modify rule |
| `ea1ad15` | Two real bugs found by running the perception server for the first time |
| `316b6f3` | Record end-to-end verification + laptop/lab env split |
| `aa35384` | **Live perception wired into bridge_node (B4)** |
| `079dcb8` | Docs: mark B4 wired |

---

## 2. Findings worth putting in the report

**Retrieval similarities were being thrown away.** `pragmabot_node.py` had
`self.ltm, _, _, _, _ = retrieve_relevant_experiences(...)`. Those four
underscores discarded exactly the numbers **Fig. 7 is computed from**. Silent
data loss — any experiment run before `1e927a6` would have been unreproducible.

**Upstream comparison (settles an old question).** Cloned
`github.com/leggedrobotics/pragmabot` (`ee68710`) and diffed:
- **11 of 12 protected modules byte-identical.** Only `scene_observer.py`
  differs, every hunk forced by the ROS 1→2 port.
- Use `diff --strip-trailing-cr` — the repo has CRLF, so a plain diff reports
  every line changed (equal `+N/-N`) and looks alarming for nothing.
- Upstream has **no** `calibration/`, `ros2_ws/`, `bags/`, `extracted/`,
  `weights/`. All ours.
- `claude_vlm_client.py`, `gemini_vlm_client.py`, `panda_skill_executor.py` are
  **ours** — the old "14 modules" rule wrongly protected them.
- **Verdict: not over-engineered.** ~3,500 added lines, ~1,900 of it
  `calibration/` — work upstream never faced (elbow camera vs our tripod).

**Perception server verified end to end** (laptop, `ea1ad15`):
```
--prompt "red cup." on extracted/red_cup/
  -> conf 0.937, 2000 points, extent 9.5 x 10.7 x 7.3 cm
```
Matches `extracted/red_cup/detections/object_pcd.npy` from the earlier manual
lab run **exactly**. Running it for the first time found two real bugs:
1. intrinsics passed as a 3x3 matrix where `backproject()` indexes a dict
2. `GROUNDED_SAM2_ROOT` hardcoded to `~/groundedsam` (lab-only path)

Both would have failed identically at the lab.

---

## 3. ROS 1 vs ROS 2 — decided, with evidence

Upstream PragmaBot is **ROS 1** (catkin, rospy). We **ported to ROS 2**
(`ament_python`, rclpy) rather than bridging.

Armin prototyped the alternative — keep the planner on ROS 1, run an HTTP
server in the ROS 2 container wrapping MoveIt (`pragmabot/armin/`, **untracked**,
in `.gitignore:74`). That approach is sound; his server is properly built
(ReentrantCallbackGroup, MultiThreadedExecutor, timeouts, cancel).

We ported because **only `scene_observer.py` touches ROS** — one file, and the
version split disappears rather than being worked around. No HTTP hop, no JSON
schema on two sides, one ROS install. A ROS 2 action also gives feedback and
cancellation for free (his `/stop` endpoint exists to cover that gap).

**Two ideas worth stealing from `armin/`:**
- `/go_home` — joint-space move to a fixed config. That is the B3 observation
  retract. **Use OUR measured pose** (`CLAUDE.md:126`), not his SRDF `ready`:
  `[-0.181, -0.420, 0.005, -2.260, -0.008, 1.806, 0.609]`
- His `grasp_pipeline.py` uses subprocesses (nothing to start up) — the
  **fallback** if the ZMQ servers prove fragile in the lab.

**Naming trap:** Armin's "bridge" = ROS1↔ROS2. Our `bridge_node.py` =
planner↔robot, both sides ROS 2. Unrelated jobs, same word.

---

## 4. What is built (and what is NOT verified)

| Piece | State |
|---|---|
| `calibration/perception_server.py` | Built. **Run on laptop CPU only.** |
| `calibration/perception_client.py` | Built. No torch/cv2 — safe in ROS 2 env. |
| `ros2_ws/.../live_perception.py` | Built. Stub-tested only. |
| `bridge_node.py` live wiring | Built. **Never run on hardware.** |
| `trial_logger.py` | Built + tested. |
| `_scene_source` injection | **MISSING — blocks every real pick** |

### `_scene_source` is deliberately None
`bridge_node` needs `(rgb, depth, intrinsics)` but does **not** subscribe to the
camera. Adding a subscription would put a second subscriber on the same
BEST_EFFORT topics the planner's `SceneObserver` already reads. It must be
injected. Until then every pick fails with a reason — by design, not a bug.

### Laptop vs lab (laptop-only, not in the repo)
- `D:/irm2pragmabot/groundedsam/.venv` — torch 2.5.1+cu121,
  **transformers==4.44.2** (5.x removed `BertModel.get_head_mask`, GroundingDINO
  breaks).
- Ran with `--device cpu` (~18 s/request). GroundingDINO's `_C` CUDA extension
  is not compiled here: NVIDIA driver present, **CUDA toolkit/nvcc absent**.
  `ms_deform_attn.py:330` takes the CUDA branch on GPU tensors and raises;
  CPU routes to the pure-PyTorch fallback already in that file.
- **The code default is still `cuda`.** Alonnisos has the toolkit — the lab runs
  the GPU path unchanged. Nothing to revert.

---

## 5. Do next, in order

1. **Inject `_scene_source`** + start both servers on the lab GPU → **first real
   pick**. Last step of B4.
2. **Gradio ground-truth control** calling `TrialLogger.label_step()`. Without a
   human label per step there is **no FP/FN confusion matrix** — that is the
   paper's 5% FP / 6.67% FN comparison. Finishes A1. No robot needed.
3. **B1 — 10 minutes at the lab.** Table plane + `fr3_hand` vs `fr3_hand_tcp`.
   Still blocks the unresolved 11 cm and every workspace number.
4. **A2 retrieval ablation** — the one genuinely offline experiment (Fig. 7).
5. **B3 observation retract** — port `/go_home` with our measured pose.

Still true: `place` uses a fixed `place_offset_xyz` (20 cm sideways). FPS is
implemented in `calibration/test_fps.py` but **not called from the loop** — so
"place on the plate" is really "place 20 cm left". Must be stated in the report.

---

## 6. Housekeeping

- **Three dead ROS 1 files** still present, unreachable (not in `setup.py`
  `scripts`): `image_decompressor_node.py`, `image_republisher_node.py`,
  `memory_manager_node.py`. Deleting them removes the last ROS 1 residue.
- `pragmabot/armin/` is **untracked and gitignored** — keep it that way.
- `extras/*.md` are now tracked in git; edit and commit them like code.
